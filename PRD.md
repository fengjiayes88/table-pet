# 桌面宠物「七七」- 产品需求文档 (PRD)

## 1. 项目概述

### 1.1 产品定位

一款基于 Electron 的 Windows 桌面宠物应用，以用户的猫咪「七七」为原型，打造一个可爱卡通风格的桌面陪伴伙伴。宠物始终显示在桌面上，在用户办公时提供陪伴感与休息提醒。

### 1.2 目标用户

- 长时间使用电脑办公的用户
- 喜欢猫咪、希望获得情感陪伴的用户
- 工作需要定时休息提醒的用户

### 1.3 核心价值

- **情感陪伴**：桌面上的猫咪带来温馨感和缓解工作压力
- **健康关怀**：久坐提醒帮助用户养成良好的工作节奏
- **低干扰**：静默运行，不打扰正常工作流程

---

## 2. 功能需求

### 2.1 宠物形象展示

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F1 | 基于用户提供的七七照片，制作可爱卡通风格的猫咪形象 | P0 |
| F2 | 宠物窗口无边框、透明背景，与桌面自然融合 | P0 |
| F3 | 窗口大小自适应屏幕分辨率，保持合适的视觉比例 | P0 |

### 2.2 窗口显示控制

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F4 | **始终置顶**：宠物窗口始终显示在其他应用程序之上 | P0 |
| F5 | 支持在设置中切换"始终置顶"或"普通窗口"模式 | P1 |
| F6 | 宠物不拦截鼠标事件（点击穿透），仅在宠物身上产生交互 | P0 |

### 2.3 交互行为

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F7 | **点击互动**：鼠标点击宠物身体，触发反应动画（如：跳起、歪头、伸爪等随机反馈） | P0 |
| F8 | **拖拽移动**：按住宠物可拖拽到桌面任意位置，松开后停留在该位置 | P0 |
| F9 | **跟随鼠标**：宠物视线/头部会跟踪鼠标光标移动方向 | P0 |
| F10 | **双击动作**：双击宠物触发特殊撒娇动画（如打滚、露肚皮） | P1 |

### 2.4 宠物动画

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F11 | **待机动画**：呼吸起伏、眨眼、偶尔摇尾巴等循环动画 | P0 |
| F12 | **随机走动**：宠物会在屏幕底部区域随机漫步，走到边缘时折返 | P0 |
| F13 | 动画切换自然流畅，不同状态之间有过渡 | P1 |
| F14 | 动画使用帧动画（Sprite Sheet）或骨骼动画，保证性能 | P0 |

### 2.5 休息提醒

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F15 | 可设置久坐提醒间隔（默认 45 分钟） | P0 |
| F16 | 到达提醒时间时，宠物做出提醒动作（如举手拍屏幕、站起来等），并显示文字气泡"该休息啦~" | P0 |
| F17 | 气泡显示数秒后自动消失，不影响正常工作 | P0 |
| F18 | 支持开启/关闭提醒功能 | P1 |

### 2.6 系统托盘

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F19 | 最小化到系统托盘，显示七七图标 | P0 |
| F20 | **右键菜单**包含以下选项： | P0 |
|      | - 设置 | |
|      | - 隐藏/显示宠物 | |
|      | - 开机自启（勾选开关） | |
|      | - 关于 | |
|      | - 退出 | |

### 2.7 设置功能

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F21 | 设置窗口简洁直观，包含以下配置项： | P0 |
|      | - 开机自启动（开关） | |
|      | - 始终置顶（开关） | |
|      | - 休息提醒间隔（下拉选择：15/30/45/60分钟 或 关闭） | |
|      | - 宠物大小（滑块：小/中/大） | |
|      | - 透明度（滑块：10%-100%） | |
| F22 | 设置项修改后即时生效并持久化保存 | P0 |

### 2.8 开机自启动

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F23 | 支持将应用注册为 Windows 开机自启动程序 | P0 |
| F24 | 管理员权限启动，写入注册表或启动文件夹 | P0 |
| F25 | 启动后最小化到系统托盘，直接显示宠物 | P1 |

### 2.9 状态持久化

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F26 | 记住宠物上次的屏幕位置，重启后恢复 | P0 |
| F27 | 记住所有设置项（提醒间隔、大小、透明度、置顶状态等） | P0 |
| F28 | 配置存储在本地用户目录，不依赖网络 | P0 |

---

## 3. 非功能需求

### 3.1 性能要求

| 编号 | 要求 | 说明 |
|------|------|------|
| NF1 | CPU 占用 | 闲置状态下 CPU 占用 < 2% |
| NF2 | 内存占用 | 内存占用 < 150MB |
| NF3 | 帧率 | 动画帧率稳定在 30fps 以上 |
| NF4 | 启动速度 | 冷启动时间 < 3 秒 |

### 3.2 兼容性

| 编号 | 要求 |
|------|------|
| NF5 | 操作系统：Windows 10 / Windows 11 |
| NF6 | 支持多显示器环境，宠物在主显示器显示 |
| NF7 | 分辨率兼容：1366x768 ~ 3840x2160 (4K) |

### 3.3 用户体验

| 编号 | 要求 |
|------|------|
| NF8 | 无音效运行，避免打扰办公环境 |
| NF9 | 界面文字为中文 |
| NF10 | 安装包体积 < 80MB |

---

## 4. 技术方案概要

### 4.1 技术栈

| 层级 | 技术选型 |
|------|----------|
| 框架 | Electron |
| 渲染 | HTML5 Canvas / CSS Animation |
| 打包 | electron-builder |
| 持久化 | electron-store (JSON 文件存储) |
| 开机自启 | electron auto-launch (注册表方式) |
| 系统托盘 | Electron Tray API |
| 语言 | TypeScript / JavaScript |

### 4.2 窗口方案

- 使用 `BrowserWindow` 创建无边框、透明窗口
- 设置 `alwaysOnTop: true` 和 `skipTaskbar: true`
- 窗口类型为 `toolbar` 或 `utility`，确保在任务栏不显示独立条目
- `setIgnoreMouseEvents` 实现非宠物区域点击穿透
- 窗口位置和尺寸信息通过 `electron-store` 持久化

### 4.3 动画方案

- 优先使用 CSS Animation + Sprite Sheet（帧动画）
- 备选方案：HTML5 Canvas 逐帧绘制
- 动画状态机管理：待机 → 走动 → 交互反馈 → 待机

### 4.4 项目结构

```
table-pet/
├── package.json
├── electron-builder.yml
├── src/
│   ├── main/           # Electron 主进程
│   │   ├── index.ts    # 入口，窗口创建
│   │   ├── tray.ts     # 系统托盘
│   │   └── auto-launch.ts  # 开机自启
│   ├── renderer/       # 渲染进程
│   │   ├── index.html
│   │   ├── pet.ts      # 宠物逻辑
│   │   ├── animation.ts # 动画管理
│   │   └── reminder.ts  # 提醒逻辑
│   └── shared/         # 共享模块
│       └── settings.ts # 设置管理
├── assets/
│   └── sprites/        # 七七精灵图资源
└── dist/               # 构建输出
```

---

## 5. 美术资源需求

| 资源 | 描述 | 规格 |
|------|------|------|
| 待机动画 | 呼吸起伏、眨眼、摇尾巴 | 精灵图，约 4-8 帧 |
| 走动动画 | 向左/右行走 | 精灵图，约 8-12 帧 |
| 点击反馈动画 | 跳起、歪头、伸爪（3-4 种随机） | 每种约 4-8 帧 |
| 撒娇动画 | 打滚、露肚皮 | 约 8-12 帧 |
| 提醒动画 | 站立举手拍屏 + 气泡 | 约 8 帧 + 气泡 UI |
| 拖拽动画 | 被拎起来的姿态 | 约 2-4 帧 |
| 托盘图标 | 小尺寸七七头像 | 16x16, 32x32, 48x48 px |
| 应用图标 | 安装程序图标 | 256x256 px (ico) |

> 素材来源：用户提供的七七照片，需要通过AI工具或手工绘制转为可爱卡通风格的精灵图。

---

## 6. 版本规划

### V1.0 - 基础陪伴版

- 宠物形象展示（无边框透明窗口）
- 待机动画
- 点击互动
- 拖拽移动
- 跟随鼠标视线
- 始终置顶
- 系统托盘 + 右键菜单
- 开机自启动
- 设置面板（大小、透明度、置顶）
- 状态持久化

### V1.1 - 行为增强版

- 随机走动
- 双击撒娇动画
- 久坐休息提醒功能

### V1.2 - 体验优化版

- 动画切换过渡优化
- 多显示器适配完善
- 更多随机动作扩充

---

## 7. 交互状态机

```
                    ┌──────────┐
          ┌────────│  启动    │
          │        └────┬─────┘
          │             ↓
          │    ┌────────────────┐
          │    │  待机 (Idle)   │ ← 默认状态
          │    └───┬───┬───┬───┘
          │        │   │   │
          │   点击↓ │拖拽↓ │定时↓
          │  ┌───┐┌───┐┌───────┐
          │  │反馈││拎起││提醒    │
          │  └─┬─┘└─┬─┘└───┬───┘
          │    │    │      │
          │    ↓    ↓      ↓
          │  ┌────────────────┐
          └──│  返回待机      │
             └────────────────┘
```

---

## 8. 风险与依赖

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| 美术素材制作耗时 | 项目延迟 | 先使用简单几何图形占位，后续替换 |
| Electron 体积较大 | 安装包偏大 | 使用 electron-builder 压缩优化 |
| 多显示器兼容 | 宠物位置异常 | 主进程监听显示器变化，自动调整 |
| 杀毒软件误报 | 自启动被拦截 | 数字签名 + 白名单 |
| Windows 更新兼容 | 自启/置顶失效 | 定期测试主流 Windows 版本 |

---

## 附录 A：术语表

| 术语 | 说明 |
|------|------|
| 精灵图 (Sprite Sheet) | 将多帧动画合并到一张图片中，通过偏移显示不同帧 |
| 点击穿透 | 鼠标事件不在宠物非身体区域被拦截，不影响下方应用 |
| 系统托盘 | Windows 任务栏右下角的图标区域 |

---

## 附录 B：参考资料

- [Electron 官方文档](https://www.electronjs.org/docs)
- [electron-builder 打包配置](https://www.electron.build/)
- [electron-store 持久化存储](https://github.com/sindresorhus/electron-store)

---

*文档版本：v1.0 | 最后更新：2026-06-11*
